import 'package:flutter/material.dart';

class YourselfPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Yourself Page'),
    );
  }
}